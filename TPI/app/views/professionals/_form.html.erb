<%= form_with(model: professional) do |form| %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
    <%= error_message(professional, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
